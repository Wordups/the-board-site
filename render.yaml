services:
  - type: web
    name: the-board-site
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python site_server.py
    healthCheckPath: /healthz
    envVars:
      - key: SITE_CACHE_TTL_SECONDS
        value: 300
